export const PLAYBACK_SPEED_OPTIONS = [0.05, 0.1, 0.25, 0.5, 1] as const;
export const DEFAULT_PLAYBACK_SPEED = 0.1;
export const MAX_SIM_DELTA_SECONDS = 0.25;
export const MISSION_DURATION_SECONDS = 24 * 60 * 60;

export const DEFAULT_CAMERA_POSITION: [number, number, number] = [4.6, 2.5, 5.2];
export const DEFAULT_CAMERA_TARGET: [number, number, number] = [0, 0, 0];

